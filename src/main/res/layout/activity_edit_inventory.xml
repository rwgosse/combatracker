<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_edit_inventory"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.myapp.combattracker.playermodule.EditInventoryActivity"
    android:background="@drawable/scroll1">

    <ListView
        android:layout_width="match_parent"
        android:id = "@+id/listView_inventory"
        android:scrollbars="none"
        android:layout_height="400sp" />

    <Button
        android:text="New Item"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/button_new_item"
        android:layout_below="@+id/listView_inventory"
        android:onClick="new_item_onclick"/>

    <Button
        android:text="New Weapon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/button_new_weapon"
        android:layout_below="@+id/listView_inventory"
        android:layout_toRightOf="@id/button_new_item"
        android:onClick="new_weapon_onclick"/>


</RelativeLayout>
